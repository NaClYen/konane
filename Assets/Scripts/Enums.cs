public enum LinkPos
{
    Up = 0,
    UpRight,
    Right,
    BottomRight,
    Bottom,
    BottomLeft,
    Left,
    UpLeft
}

public enum ChessType
{
    Black,
    White
}

public enum HintType
{
    CanAttack,  // 可以進攻
    CanGoTo,    // 可到達
}